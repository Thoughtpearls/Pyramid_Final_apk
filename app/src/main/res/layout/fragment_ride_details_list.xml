<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    android:id="@+id/swipeRefreshLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:visibility="gone"
        android:name="com.thoughtpearl.conveyance.ui.ridedetails.RideDetailsFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="LinearLayoutManager"
        tools:context=".ui.rides.RideListingFragment"
        tools:listitem="@layout/fragment_ride_details"
        android:background="@color/background"
        android:layout_marginBottom="60dp">
    </androidx.recyclerview.widget.RecyclerView>
    <TextView
        android:id="@+id/emptyRideTextView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Today you have no rides"
        android:textColor="@color/white"
        android:gravity="center"
        android:layout_gravity="center"
        android:textSize="22sp"
        android:visibility="gone"
        android:fontFamily="@font/epilogue_regular">
    </TextView>
        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/errorAnimation"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:lottie_autoPlay="true"
            app:lottie_loop="true"
            app:lottie_rawRes="@raw/pageerror"
            tools:ignore="MissingConstraints" />
</LinearLayout>
</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>